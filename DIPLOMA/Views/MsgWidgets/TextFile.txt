function displayContent(index) {
        let donate = donateMsgs[index];
        let contenttext = headerTextTemplate;


        contenttext = contenttext.replace('{amount}', donate.amount).replace('{user}', donate.donatorName);

        console.log(scrollingSpeed);
        document.getElementById("slidercontent").innerHTML = contenttext;
        index++;
        if () {
            index = 0;
        }
        setTimeout(function () {
            displayContent(index)
        }, scrollingSpeed * 1000);
    }